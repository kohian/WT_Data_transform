version: 2

sources:
  - name: wt_bigquery #can be anything is waht you are calling the sources
    schema: RAW_TEST
    tables:
      - name: fs_members
        # identifier: fs_members # actual name in BQ not needed if i don't mind same
        # config:
        #   loaded_at_field: ingested_at
          # freshness:
          #   warn_after: {count: 1, period: hour}
          #   error_after: {count: 24, period: hour}
      - name: fs_rosters
      - name: fs_blockouts
